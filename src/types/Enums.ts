export enum Currency {
  EUR = "EUR",
  USD = "USD",
  UAH = "UAH",
}

export enum RepeatUnit {
  NONE = "NONE",
  DAY = "DAY",
  WEEK = "WEEK",
  MONTH = "MONTH",
  QUARTER = "QUARTER",
  YEAR = "YEAR",
}

export enum PaymentUpdateMode {
  SINGLE = "SINGLE",
  FUTURE = "FUTURE",
  ALL = "ALL",
}

export enum PaymentType {
  INCOME = "INCOME",
  EXPENSE = "EXPENSE",
  TRANSFER = "TRANSFER",
  BUDGET = "BUDGET",
}
